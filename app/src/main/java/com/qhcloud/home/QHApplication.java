package com.qhcloud.home;import android.app.Application;import com.qhcloud.home.entity.Constant;import com.qhcloud.home.util.AppUtil;import com.qhcloud.home.util.CrashHandler;public class QHApplication extends Application {    private static final String TAG = "QHApplication1";    @Override    public void onCreate() {        super.onCreate();        //debug 版本时，不需要处理异常        if (!AppUtil.isApkDebugAble(getApplicationContext())) {            CrashHandler.getInstance().init(this);            Constant.DEBUG = false;        }    }}