<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingBottom="8dp"
    android:paddingTop="8dp">

    <TextView
        android:id="@+id/chat_to_image_date_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_gravity="center"
        android:background="@drawable/bg_chat_date_black"
        android:padding="4dp"
        android:text="2017-04-19"
        android:textColor="@color/colorWhite"
        android:textSize="12sp" />


    <com.qhcloud.demo.view.CircleImageView
        android:id="@+id/chat_to_image_avatar_iv"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_alignParentRight="true"
        android:layout_below="@id/chat_to_image_date_tv"
        android:layout_marginTop="8dp"
        android:contentDescription="@null"
        android:scaleType="centerCrop"
        android:src="@mipmap/ic_launcher"
        app:border_color="@color/colorTransparent"
        app:border_width="0dp" />

    <ImageView
        android:id="@+id/chat_to_image_content_iv"
        android:layout_width="240dp"
        android:layout_height="240dp"
        android:layout_below="@id/chat_to_image_date_tv"
        android:layout_marginRight="8dp"
        android:layout_marginTop="8dp"
        android:layout_toLeftOf="@id/chat_to_image_avatar_iv"
        android:src="@mipmap/ic_launcher"
        android:scaleType="centerCrop"
        android:background="#EEEEEE"/>

</RelativeLayout>